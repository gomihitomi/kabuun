services:
  dev:
    container_name: dev
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    command: "/bin/bash"
    tty: true
    restart: always
    volumes:
      - ..:/workspace:cached
